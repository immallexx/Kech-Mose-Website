# Normalize all text files to LF in the repository (and on checkout)
* text=auto eol=lf

# Binary assets — never alter
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
